{
  "name": "dilg-region10-calendar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js",
    "test:e2e": "playwright test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.1.0",
    "node-ical": "^0.26.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
